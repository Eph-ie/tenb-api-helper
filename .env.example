# Copy to .env, fill in, then: source .env
# (.env is gitignored — never commit real keys)
export TIO_ACCESS_KEY="your-access-key-here"
export TIO_SECRET_KEY="your-secret-key-here"
# Only if you are not on the default US cloud:
# export TIO_BASE_URL="https://cloud.tenable.com"
